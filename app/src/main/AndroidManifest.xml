<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- permissões do app -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vistomaisandroid"
        tools:targetApi="31">
        <activity
            android:name=".views.ProprietariosActivity"
            android:exported="false" />
        <activity
            android:name=".views.CadastroProprietarioActivity"
            android:exported="false" />
        <activity
            android:name=".views.CadastroVeiculoActivity"
            android:exported="false" />
        <activity
            android:name=".views.VeiculosActivity"
            android:exported="false" /> <!-- Tela de login -->
        <activity
            android:name=".views.LoginActivity"
            android:exported="false" /> <!-- SplashScrenn -->
        <activity
            android:name=".views.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Tela inicial do app(tela home) -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>